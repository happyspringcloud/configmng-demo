eureka:
  instance: 
    hostname: ${HOSTNAME}.eureka-springcloud 
  client: 
    register-with-eureka: true
    fetch-registry: true
    service-url: 
      defaultZone: ${eureka_servers:http://localhost:8761/eureka}
